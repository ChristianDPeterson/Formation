query userList {
	getAllUsers {
		id
		username
		email
	}
}

# mutation addSet($name: String!, $year: String!, $numParts: Int!) {
# 	addSet(name: $name, year: $year, numParts: $numParts) {
# 		id
# 		name
# 		numParts
# 		year
# 	}
# }
